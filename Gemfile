source 'https://rubygems.org'

ruby '2.4.1'

gem 'rspec'
gem 'pry'
gem 'rubocop'
gem 'simplecov'
gem 'simplecov-console'
gem 'httparty'

purchases_data[:data].each{|datapoint|
m_s.has_key?(datapoint[:item]) ? m_s[(datapoint[:item])]
<< (datapoint[:spend].to_i) : m_s[(datapoint[:item])] = Array.new(datapoint[:spend].to_i}
